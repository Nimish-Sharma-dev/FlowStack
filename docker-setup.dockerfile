# Dockerfile.backend - Backend container configuration\n\nFROM node:18-alpine\n\nWORKDIR /app\n\n# Copy package files\nCOPY package.json package-lock.json ./\n\n# Install dependencies\nRUN npm ci --only=production\n\n# Copy application\nCOPY . .\n\n# Expose port\nEXPOSE 5000\n\n# Health check\nHEALTHCHECK --interval=30s --timeout=10s --start-period=40s --retries=3 \\\n  CMD node -e \"require('http').get('http://localhost:5000/health', (r) => {if (r.statusCode !== 200) throw new Error(r.statusCode)})\"\n\n# Start application\nCMD [\"npm\", \"start\"]\n\n---\n\n# Dockerfile.frontend - Frontend container configuration\n\nFROM node:18-alpine AS builder\n\nWORKDIR /app\n\nCOPY package.json package-lock.json ./\n\nRUN npm ci\n\nCOPY . .\n\nRUN npm run build\n\n# Production stage\nFROM nginx:alpine\n\n# Copy built application\nCOPY --from=builder /app/dist /usr/share/nginx/html\n\n# Copy nginx configuration\nCOPY nginx.conf /etc/nginx/conf.d/default.conf\n\nEXPOSE 3000\n\nCMD [\"nginx\", \"-g\", \"daemon off;\"]\n\n---\n\n# nginx.conf - Nginx configuration for frontend\n\nserver {\n  listen 3000;\n  server_name _;\n\n  location / {\n    root /usr/share/nginx/html;\n    try_files $uri $uri/ /index.html;\n  }\n\n  location /api {\n    proxy_pass http://backend:5000;\n    proxy_http_version 1.1;\n    proxy_set_header Upgrade $http_upgrade;\n    proxy_set_header Connection 'upgrade';\n    proxy_set_header Host $host;\n    proxy_cache_bypass $http_upgrade;\n  }\n}\n\n---\n\n# docker-compose.yml - Complete stack orchestration\n\nversion: '3.8'\n\nservices:\n  mongodb:\n    image: mongo:6\n    container_name: vwp-mongodb\n    environment:\n      MONGO_INITDB_ROOT_USERNAME: admin\n      MONGO_INITDB_ROOT_PASSWORD: password\n      MONGO_INITDB_DB: visual-workspace\n    ports:\n      - \"27017:27017\"\n    volumes:\n      - mongo-data:/data/db\n      - mongo-config:/data/configdb\n    networks:\n      - vwp-network\n    healthcheck:\n      test: echo 'db.runCommand(\"ping\").ok' | mongosh localhost:27017/test --quiet\n      interval: 10s\n      timeout: 5s\n      retries: 5\n\n  redis:\n    image: redis:7-alpine\n    container_name: vwp-redis\n    ports:\n      - \"6379:6379\"\n    volumes:\n      - redis-data:/data\n    networks:\n      - vwp-network\n    healthcheck:\n      test: [\"CMD\", \"redis-cli\", \"ping\"]\n      interval: 10s\n      timeout: 5s\n      retries: 5\n\n  backend:\n    build:\n      context: ./backend\n      dockerfile: ../Dockerfile.backend\n    container_name: vwp-backend\n    environment:\n      NODE_ENV: production\n      PORT: 5000\n      MONGO_URI: mongodb://admin:password@mongodb:27017/visual-workspace\n      JWT_SECRET: ${JWT_SECRET:-change-me-in-production}\n      REDIS_URL: redis://redis:6379\n      SOCKET_IO_ORIGIN: http://localhost:3000\n    ports:\n      - \"5000:5000\"\n    depends_on:\n      mongodb:\n        condition: service_healthy\n      redis:\n        condition: service_healthy\n    networks:\n      - vwp-network\n    restart: unless-stopped\n\n  frontend:\n    build:\n      context: ./frontend\n      dockerfile: ../Dockerfile.frontend\n    container_name: vwp-frontend\n    ports:\n      - \"3000:3000\"\n    depends_on:\n      - backend\n    networks:\n      - vwp-network\n    restart: unless-stopped\n\nvolumes:\n  mongo-data:\n  mongo-config:\n  redis-data:\n\nnetworks:\n  vwp-network:\n    driver: bridge\n