# Visual Workspace Platform - Complete Setup & Implementation Guide\n\n## 🚀 Quick Start\n\n### Prerequisites\n- Node.js 18+ installed\n- MongoDB running locally or connection string ready\n- Git for version control\n- Postman or similar for API testing\n\n### 1. Project Initialization\n\n```bash\n# Create project directory\nmkdir visual-workspace-platform\ncd visual-workspace-platform\n\n# Initialize git\ngit init\n\n# Create main project structure\nmkdir backend frontend shared\ncd backend && npm init -y\ncd ../frontend && npm init -y\ncd ..\n```\n\n### 2. Backend Setup\n\n```bash\ncd backend\n\n# Install dependencies\nnpm install\n\n# Copy the backend server file\ncp ../backend-server.js ./server.js\n\n# Create .env file\ncat > .env << 'EOF'\nNODE_ENV=development\nPORT=5000\nMONGO_URI=mongodb://localhost:27017/visual-workspace\nJWT_SECRET=your-super-secret-jwt-key-change-in-production\nJWT_EXPIRE=7d\nREDIS_URL=redis://localhost:6379\nSOCKET_IO_ORIGIN=http://localhost:3000\nEOF\n\n# Start backend\nnpm run dev\n```\n\n### 3. Frontend Setup\n\n```bash\ncd ../frontend\n\n# Install dependencies\nnpm install\n\n# Create vite.config.js\ncat > vite.config.js << 'EOF'\nimport { defineConfig } from 'vite'\nimport react from '@vitejs/plugin-react'\n\nexport default defineConfig({\n  plugins: [react()],\n  server: {\n    port: 3000,\n    proxy: {\n      '/api': {\n        target: 'http://localhost:5000',\n        changeOrigin: true,\n        rewrite: (path) => path.replace(/^\\/api/, '/api'),\n      },\n    },\n  },\n})\nEOF\n\n# Create index.html\ncat > index.html << 'EOF'\n<!doctype html>\n<html lang=\"en\">\n  <head>\n    <meta charset=\"UTF-8\" />\n    <link rel=\"icon\" type=\"image/svg+xml\" href=\"/favicon.svg\" />\n    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n    <title>Visual Workspace - Collaborative Planning Platform</title>\n  </head>\n  <body>\n    <div id=\"root\"></div>\n    <script type=\"module\" src=\"/src/index.jsx\"></script>\n  </body>\n</html>\nEOF\n\n# Create src/index.jsx\ncat > src/index.jsx << 'EOF'\nimport React from 'react'\nimport ReactDOM from 'react-dom/client'\nimport App from './App'\nimport './styles/global.css'\n\nReactDOM.createRoot(document.getElementById('root')).render(\n  <React.StrictMode>\n    <App />\n  </React.StrictMode>,\n)\nEOF\n\n# Create .env file\ncat > .env << 'EOF'\nVITE_API_URL=http://localhost:5000\nVITE_SOCKET_URL=http://localhost:5000\nVITE_ENV=development\nEOF\n\n# Start frontend\nnpm run dev\n```\n\n## 📁 File Organization After Setup\n\n```\nvisual-workspace-platform/\n├── backend/\n│   ├── server.js (main backend server)\n│   ├── package.json\n│   ├── .env\n│   └── node_modules/\n│\n├── frontend/\n│   ├── src/\n│   │   ├── App.jsx (main app component)\n│   │   ├── index.jsx\n│   │   ├── components/\n│   │   │   ├── Canvas.jsx\n│   │   │   ├── Block.jsx\n│   │   │   ├── Layout/\n│   │   │   ├── Dashboard/\n│   │   │   └── ...\n│   │   ├── pages/\n│   │   ├── store/\n│   │   ├── context/\n│   │   ├── services/\n│   │   ├── styles/\n│   │   └── ...\n│   ├── index.html\n│   ├── vite.config.js\n│   ├── package.json\n│   ├── .env\n│   └── node_modules/\n│\n└── README.md\n```\n\n## 🔧 Creating Core Components Step by Step\n\n### Step 1: Authentication System\n\nCreate `backend/auth-middleware.js`:\n```javascript\nconst jwt = require('jsonwebtoken');\n\nconst authMiddleware = (req, res, next) => {\n  const token = req.headers.authorization?.split(' ')[1];\n  \n  if (!token) {\n    return res.status(401).json({ error: 'No token provided' });\n  }\n  \n  try {\n    const decoded = jwt.verify(token, process.env.JWT_SECRET);\n    req.user = decoded;\n    next();\n  } catch (error) {\n    res.status(401).json({ error: 'Invalid token' });\n  }\n};\n\nmodule.exports = authMiddleware;\n```\n\nCreate `frontend/src/services/api.js`:\n```javascript\nimport axios from 'axios';\n\nconst API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000';\n\nconst api = axios.create({\n  baseURL: `${API_URL}/api`,\n});\n\n// Add token to requests\napi.interceptors.request.use((config) => {\n  const token = localStorage.getItem('token');\n  if (token) {\n    config.headers.Authorization = `Bearer ${token}`;\n  }\n  return config;\n});\n\n// Handle token expiration\napi.interceptors.response.use(\n  (response) => response,\n  (error) => {\n    if (error.response?.status === 401) {\n      localStorage.removeItem('token');\n      window.location.href = '/login';\n    }\n    return Promise.reject(error);\n  }\n);\n\nexport default api;\n```\n\n### Step 2: Create Landing Page\n\nCreate `frontend/src/pages/LandingPage.jsx`:\n```javascript\nimport React from 'react';\nimport { useNavigate } from 'react-router-dom';\nimport HeroSection from '../components/Landing/HeroSection';\nimport FeaturesSection from '../components/Landing/FeaturesSection';\n\nconst LandingPage = () => {\n  const navigate = useNavigate();\n\n  return (\n    <div className=\"landing-page\">\n      <HeroSection onCTA={() => navigate('/signup')} />\n      <FeaturesSection />\n    </div>\n  );\n};\n\nexport default LandingPage;\n```\n\n### Step 3: Create Dashboard\n\nCreate `frontend/src/pages/DashboardPage.jsx`:\n```javascript\nimport React, { useEffect, useState } from 'react';\nimport { useNavigate } from 'react-router-dom';\nimport api from '../services/api';\n\nconst DashboardPage = () => {\n  const navigate = useNavigate();\n  const [projects, setProjects] = useState([]);\n  const [loading, setLoading] = useState(true);\n\n  useEffect(() => {\n    fetchProjects();\n  }, []);\n\n  const fetchProjects = async () => {\n    try {\n      const response = await api.get('/projects');\n      setProjects(response.data);\n    } catch (error) {\n      console.error('Error fetching projects:', error);\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  const createProject = async (projectData) => {\n    try {\n      const response = await api.post('/projects', projectData);\n      setProjects([...projects, response.data]);\n      navigate(`/project/${response.data._id}`);\n    } catch (error) {\n      console.error('Error creating project:', error);\n    }\n  };\n\n  return (\n    <div className=\"dashboard-page\">\n      <h1>Your Projects</h1>\n      {loading ? (\n        <div>Loading...</div>\n      ) : (\n        <div className=\"projects-grid\">\n          {projects.map((project) => (\n            <div\n              key={project._id}\n              className=\"project-card\"\n              onClick={() => navigate(`/project/${project._id}`)}\n            >\n              <h2>{project.name}</h2>\n              <p>{project.description}</p>\n            </div>\n          ))}\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default DashboardPage;\n```\n\n### Step 4: Create Project Editor\n\nCreate `frontend/src/pages/ProjectPage.jsx`:\n```javascript\nimport React, { useEffect } from 'react';\nimport { useParams } from 'react-router-dom';\nimport { useDispatch, useSelector } from 'react-redux';\nimport Canvas from '../components/Canvas';\nimport TopToolbar from '../components/Layout/TopToolbar';\nimport SidebarNav from '../components/Layout/SidebarNav';\nimport RightPanel from '../components/Layout/RightPanel';\nimport api from '../services/api';\nimport { setCurrentProject } from '../store/slices/projectSlice';\nimport { setBlocks, setConnections } from '../store/slices/canvasSlice';\n\nconst ProjectPage = () => {\n  const { projectId } = useParams();\n  const dispatch = useDispatch();\n  const { user } = React.useContext(require('../context/AuthContext'));\n  const project = useSelector((state) => state.project.currentProject);\n\n  useEffect(() => {\n    fetchProject();\n  }, [projectId]);\n\n  const fetchProject = async () => {\n    try {\n      const response = await api.get(`/projects/${projectId}`);\n      dispatch(setCurrentProject(response.data));\n      dispatch(setBlocks(response.data.blocks));\n      dispatch(setConnections(response.data.connections));\n    } catch (error) {\n      console.error('Error fetching project:', error);\n    }\n  };\n\n  return (\n    <div className=\"project-page\">\n      <TopToolbar projectName={project?.name} />\n      <div className=\"project-content\">\n        <SidebarNav />\n        <Canvas projectId={projectId} userId={user._id} userName={user.name} />\n        <RightPanel />\n      </div>\n    </div>\n  );\n};\n\nexport default ProjectPage;\n```\n\n## 🗄️ Database Setup\n\nCreate `backend/db-init.js` for initialization:\n```javascript\nconst mongoose = require('mongoose');\n\nmongoose.connect(process.env.MONGO_URI);\n\nconst db = mongoose.connection;\n\ndb.on('error', (error) => {\n  console.error('Database connection error:', error);\n});\n\ndb.on('open', () => {\n  console.log('Database connected successfully');\n});\n\nmodule.exports = db;\n```\n\nRun initial setup:\n```bash\ncd backend\nnode db-init.js\n```\n\n## 🔌 WebSocket Integration\n\nThe server includes Socket.IO for real-time collaboration. Connections are handled in the main server.js file:\n\n```javascript\nio.on('connection', (socket) => {\n  // User joins project\n  socket.on('join_project', (data) => {\n    socket.join(`project:${data.projectId}`);\n  });\n\n  // Real-time events\n  socket.on('block_created', (data) => {\n    io.to(`project:${data.projectId}`).emit('block_created', data.block);\n  });\n});\n```\n\n## 🎨 Styling Implementation\n\n1. Create all CSS files in `frontend/src/styles/`:\n   - `global.css` - Global styles\n   - `variables.css` - CSS variables\n   - `animations.css` - Animations\n   - `components/canvas.css` - Canvas styling\n   - `components/blocks.css` - Block styling\n\n2. Import in App.jsx:\n```javascript\nimport './styles/global.css';\nimport './styles/variables.css';\nimport './styles/animations.css';\n```\n\n## 🚀 Running the Application\n\n```bash\n# Terminal 1: Backend\ncd backend\nnpm run dev\n# Backend runs on http://localhost:5000\n\n# Terminal 2: Frontend\ncd frontend\nnpm run dev\n# Frontend runs on http://localhost:3000\n\n# Terminal 3: MongoDB (if running locally)\nmongod\n```\n\nAccess the app at `http://localhost:3000`\n\n## 🧪 Testing the API\n\nUse Postman to test endpoints:\n\n1. **Sign Up**\n   - POST `http://localhost:5000/api/auth/signup`\n   - Body: `{ \"email\": \"test@example.com\", \"password\": \"password\", \"name\": \"Test User\" }`\n\n2. **Login**\n   - POST `http://localhost:5000/api/auth/login`\n   - Body: `{ \"email\": \"test@example.com\", \"password\": \"password\" }`\n   - Returns: token\n\n3. **Create Project**\n   - POST `http://localhost:5000/api/projects`\n   - Headers: `Authorization: Bearer <token>`\n   - Body: `{ \"name\": \"My Project\", \"description\": \"Test project\" }`\n\n4. **Get Projects**\n   - GET `http://localhost:5000/api/projects`\n   - Headers: `Authorization: Bearer <token>`\n\n## 📦 Environment Secrets to Change Before Production\n\n1. Change `JWT_SECRET` in `.env`\n2. Use production MongoDB URI\n3. Enable HTTPS\n4. Set proper CORS origins\n5. Configure environment-specific settings\n\n## 🔄 Next Steps for Full Implementation\n\n1. **Implement remaining pages**: LoginPage, SignupPage, SettingsPage\n2. **Create layout components**: MainLayout, TopToolbar, SidebarNav, RightPanel\n3. **Add data persistence**: Implement autosave and offline sync\n4. **Real-time features**: Complete presence indicators, live cursors\n5. **Advanced features**: Comments, version history, exports\n6. **Performance**: Implement caching, lazy loading, virtualization\n7. **Testing**: Unit tests, integration tests, E2E tests\n8. **Deployment**: Docker setup, CI/CD pipeline\n\n## 📚 Additional Resources\n\n- [React Documentation](https://react.dev)\n- [Express.js Guide](https://expressjs.com/)\n- [Socket.io Docs](https://socket.io/docs/)\n- [MongoDB Manual](https://docs.mongodb.com/manual/)\n- [Redux Documentation](https://redux.js.org/)\n\n## 🆘 Troubleshooting\n\n### CORS Issues\nEnsure `SOCKET_IO_ORIGIN` in backend `.env` matches your frontend URL.\n\n### MongoDB Connection\nMake sure MongoDB is running. Check with:\n```bash\nmongosh  # or mongo in older versions\n```\n\n### Port Already in Use\nChange port in `.env` or kill process using the port:\n```bash\nlsof -ti:5000 | xargs kill -9  # Unix/Mac\nnetstat -ano | findstr :5000   # Windows\n```\n\n## ✅ Checklist for Complete Implementation\n\n- [ ] Backend server running\n- [ ] Frontend development server running\n- [ ] Database connected\n- [ ] Authentication working (signup/login)\n- [ ] Dashboard displays projects\n- [ ] Canvas loads project data\n- [ ] Blocks can be created and edited\n- [ ] Real-time updates working\n- [ ] Styling complete\n- [ ] Mobile responsiveness added\n- [ ] Error handling implemented\n- [ ] Testing suite created\n- [ ] Documentation written\n- [ ] Performance optimized\n- [ ] Ready for deployment\n